# Some basic OSX GIT ignore rules

*.[oa]    # no .a or .o files
!lib.a    # but do track lib.a, even though you're ignoring .a files above
*~        # nothing that ends in a tilde
/TODO     # only ignore the root TODO file, not subdir/TODO
build/    # ignore all files in the build/ directory
*.db      # ignore all database files
*.sqlite3	# ignore all sqlite3 files
*.swp     # no swap files
.DS_Store # ignore the OSX finder info

/.bundle/
/.rdebugrc
/coverage/
/db/development.sqlite3
/db/test.sqlite3
/rdebugnotes.txt